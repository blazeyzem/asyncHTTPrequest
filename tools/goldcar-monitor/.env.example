# Sekrety monitora. Skopiuj do .env (chmod 600) - plik jest w .gitignore.
# Gmail wymaga hasla aplikacji (Google Account -> Bezpieczenstwo -> Hasla do aplikacji),
# zwykle haslo do konta nie zadziala przy wlaczonej weryfikacji dwuetapowej.
SMTP_USER=blazeyzem@gmail.com
SMTP_PASS=xxxxxxxxxxxxxxxx
